appId: com.a1lab.echos
---
# Helper: Launch app with clean state and mic permission
- launchApp:
    clearState: true
    permissions:
      notifications: allow
      microphone: allow

# Grant microphone permission when prompted (fallback for system dialog)
- runFlow: helpers/grant_permissions.yaml
