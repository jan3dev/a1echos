# Python env for scripts/keyboard-lm/bench.py (and build-spike-model.sh).
# transformers v5 drops the GPTNeoX rotary_pct key the GGUF converter reads,
# so stay on 4.x — same pin as build-spike-model.sh.
torch
transformers==4.57.6

# Optional: needed only when --model/--baseline is a .gguf
# llama-cpp-python
